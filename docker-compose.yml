
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tecsup-rooms-frontend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
